# 5회차 - 법령 이름을 몰라도 답하기(본문 의미검색) + 화면 재구성(신호·근거·편의)

> 이 문서는 **현재 프로덕션 상태를 반영한 구현계획**이다.
> 이번 회차는 **백엔드(법령 검색 강화)를 먼저** 하고, **화면(UI) 재구성은 그 뒤에** 한다.

| 항목 | 내용 |
|------|------|
| 날짜 | 미정 (예정) |
| 이번 주제 | **[전반부·백엔드]** 법 이름 없이도 관련 법을 찾는 **본문 의미검색(aiSearch) 내장** · **[후반부·화면]** 숨은 신호 표시 · 근거 패널 재구성 · 생성 제어·편의 |
| 선행 상태 | 검색·재정렬·법령 분기·약칭·인용 검증·**판례 조회**(백엔드)는 **개발완료**. 근거 패널(말풍선형)·출처 색뱃지는 **부분 적용** |

> **이번 회차 한 줄 요약**
> 먼저 **검색의 큰 구멍**을 메운다 — 지금은 사용자가 "근로기준법"처럼 **법 이름을 정확히 대야만** 법령을 찾는다. "야근수당 안 주면" 같이 **이름 없이 상황만 물으면 못 찾는다.** 그래서 **법 이름 없이 상황으로 법을 찾는 검색(aiSearch)** 을 백엔드에 내장한다(법제처가 의미검색을 해 주므로 우리가 법을 따로 저장할 필요 없음 — 실측 검증 완료). 그 다음 **화면**에서, 서버가 이미 보내는데 화면이 흘리던 신호(분기·인용 검증)를 드러내고, 근거 보는 방식을 패널로 정리하고, 복사·중단 같은 편의를 더한다.

---

## 0. 출발점 — 지금 프로덕션은 어디까지 와 있나 (가장 먼저 볼 것)

**지금 코드가 실제로 어디까지 와 있는지**를 네 갈래로 나눠 본다. 이 표가 이번 회차의 모든 근거다.

### 0-1. 이미 되어 있는 것 ✅

| 항목 | 구현 상태 | 위치 |
|---|---|---|
| 근거 → 우측 패널(아코디언 대체) | ✅ 됨 — **말풍선형 패널**(넓은 화면 오른쪽 절반 / 좁은 화면 바텀시트, X·바깥·Esc로 닫힘) | `components/ui/source-panel.tsx` (신규) |
| 출처 뱃지 📘 내부규정 / ⚖️ 법령 | ✅ **패널 안에만** 색뱃지 적용(📘 파랑 / ⚖️ 보라) | `source-panel.tsx`의 `BADGE` |
| 법령 카드에 법제처 원문 링크 | ✅ 됨 (`law.go.kr/법령/{법령명}`) | `source-panel.tsx:66` |

### 0-2. 그 밖에 이미 들어와 있는 것 🆕

| 새로 들어온 것 | 무엇인가 | 위치 |
|---|---|---|
| **판례·헌재결정 조회** | 법령 분기 시 법령과 **함께 판례를 병렬 조회**해 근거·LLM 컨텍스트로 보강. 상위 2건 본문까지 회수 | `lib/law/decisions.ts` (신규), `app/api/chat/route.ts:200-230` |
| **법령 모듈 리팩터(전송·캐시 분리)** | 법제처 호출을 **공통 전송 계층**으로 모으고(법령·판례·헌재·해석례 한 빌더), **law_cache 캐싱**(OC키 제거·TTL)을 붙임 | `lib/law/client.ts`·`lib/law/cache.ts` (둘 다 신규) |
| **판례 출처 종류 추가** | 참조문서/패널에 `kind: "precedent"` ⚖️ 판례 카드 추가 | `source-panel.tsx`, `page.tsx`의 `SourceItem` |
| **프롬프트 보강** | 판례 컨텍스트·법령 근거 반영 | `lib/ai/prompts.ts` |
| **판례 파서 단위검증 스크립트** | 네트워크 없이 픽스처로 파서 검증 | `scripts/test-law-decisions.ts` |

> **핵심**: 법령 모듈은 전송·캐시 계층이 분리돼 있고 **판례 조회까지 붙어 있다.** 후반부 화면 작업은 이 판례 근거까지 한 화면에서 일관되게 보여 주도록 맞춘다.

### 0-3. 아직 안 된 것 ❌ (이번 회차 후반부)

| 항목 | 현재 | 근거 |
|---|---|---|
| `routing`(분기 결과) 이벤트 **화면 소비** | ❌ **여전히 안 받음** — 서버는 보내는데 화면 타입에 없음 | `page.tsx:17-21`(sources·delta·done·error만), 서버는 `route.ts:256-261`에서 송출 |
| `citations`(인용 검증) **화면 소비** + 환각 경고 배너 | ❌ 안 받음 — **공들인 환각 차단이 사용자에게 안 보임** | `route.ts:266-279`에서 송출하나 화면 미수신 |
| 분기 줄("📘 내부규정 근거 / ⚖️ 법령 근거 관련도 N%") | ❌ 없음 | — |
| 본문 인용 마커 `[1][2]` → 패널 점프 | ❌ 없음 — 참조 리스트 클릭으로만 패널 열림 | `page.tsx`의 `Sources`/`SourceItem` |
| 인용 검증 배지(✓/✗/⚠)를 법령 카드에 | ❌ 없음 | `source-panel.tsx`에 검증 표시 없음 |
| 출처 색뱃지를 **참조 리스트에도** | ❌ 리스트는 아직 회색 텍스트 뱃지("법제처 법령"/"판례"/"N% 관련도") | `page.tsx:419-440` |
| 공용 타입 `lib/types.ts` | ❌ 없음 — 화면·서버가 `StreamEvent`를 **각자 따로** 정의(중복) | `page.tsx:17`·`route.ts:37` |
| 편의 4종: 복사·예시칩·생성중단·맨아래로 | ❌ 없음 (스크롤 추적만 있고 버튼 없음, 중단 불가) | `page.tsx` |
| 진행 상태(`stage` 이벤트) | ❌ 없음 — 서버에 `stage` 이벤트 자체가 없음 | `route.ts:37-43` |

### 0-4. 왜 "법령 이름을 안 댄 질의"가 지금까지 안 됐나 (전반부의 뿌리)

이번 회차 전반부(백엔드)를 이해하려면 **지금 왜 안 되는지**부터 봐야 한다. 어드바이저는 **법 이름을 정확히 말한 질의만** 법령 근거를 찾는다. 원인은 한 곳, `lib/law/search.ts`의 `searchLaw`가 **법령 "이름"으로만 검색**하기 때문이다.

현재 동작 (코드 그대로):

```
질의 → extractLawNames(query)        ← 정규식으로 "○○법/시행령/규칙…" 토큰 추출 (search.ts:43, 175)
        │
        ├─ 법령명 뽑힘("근로기준법") → 법제처 target=law 검색 → ✅ OK
        └─ 못 뽑음(법 이름 없는 자연어)
              → terms = [query]        ← 자연어 전체를 검색어로 (search.ts:176)
              → lawSearch.do?target=law 는 "법령 이름" 기준 검색 → 0건
              → refs.length===0 → 빈 결과 반환 (search.ts:192) → 법령 근거 없이 답
```

| 질의 | `extractLawNames` | `target=law` 결과 |
|---|---|---|
| "근로기준법 연차 며칠?" | `["근로기준법"]` | ✅ 법령 조회 |
| "야근수당 안 주면 불법인가요?" | `[]` (법 이름 없음) | ❌ **0건** (실측 `totalCnt:"0"`) |
| "전세보증금 못 돌려받았어요" | `[]` | ❌ 0건 |

**근본 원인 두 가지:**

1. **법제처 `target=law`는 법령 "제목(이름)" 검색**이라 조문 "내용"으로는 못 찾는다. "야근수당"이라는 말은 근로기준법 **본문**엔 있어도 **법령명**엔 없으니 0건이 된다.
2. PIMS는 korean-law MCP의 도구 중 **`search_law`(제목 검색)만 내장**하고, **`search_ai_law`(조문 본문 의미검색)는 내장하지 않았다.** 그래서 "법 이름을 모르는 질의 → 법령"으로 이어 주는 경로 자체가 처음부터 없었다.

> 결국 **법을 잘 아는 사람일수록 잘 쓰이고, 정작 법 이름을 모르는 외부 기관 담당자(주 사용자)일수록 법령 근거를 못 받는** 역설이 있었다. 이번 회차 전반부(2장)는 빠져 있던 **본문 의미검색(`aiSearch`)** 을 내장해 이 구멍을 메운다.

---

## 1. 이번 회차의 순서 (백엔드 먼저, 화면 나중)

| 순서 | 묶음 | 무엇을 | 왜 이 순서인가 |
|---|---|---|---|
| **전반부** | **2장 [백엔드] 본문 의미검색(aiSearch) 내장** | 법 이름 없이 상황만 물어도 관련 법을 찾는다 | **검색 품질의 가장 큰 구멍.** 화면을 아무리 다듬어도 검색이 못 찾으면 의미 없다. 그래서 먼저. |
| **후반부** | **3장 [화면] 숨은 신호 드러내기** | 분기·인용 검증 결과를 화면에 표시 + 공용 타입 | 서버가 이미 보내는 걸 받기만 하면 됨(체감 큼) |
| | **4장 [화면] 근거 보여 주는 방식 재구성** | 패널 뱃지 통일·판례 카드 정합·본문 `[1][2]` 점프·검증 배지 | 새로 들어온 판례까지 한 화면에서 일관되게 |
| | **5장 [화면] 쓰기 편하게** | 복사·예시 질문·생성 중단·맨 아래로 | 비전문가 사용자의 작은 매끄러움 |

---

# 전반부 — 백엔드

## 2. [백엔드] 법 이름을 몰라도 찾기 — 본문 의미검색(aiSearch) 내장

### 2-1. 무엇을 고치나

`searchLaw`(`search.ts`)가 법 **이름**으로만 검색해서, 이름 없는 자연어 질의는 0건이 된다(0-4 참조). 여기에 **법 이름 없이 상황으로 조문을 찾는 검색**을 더해, **이름 검색이 0건일 때 자동으로 이어 받게** 한다.

### 2-2. 해법 — 법제처 `aiSearch`

법제처에는 `search_law`(제목 검색)와 별개로 **`aiSearch`(본문 의미검색)** 엔드포인트가 있다. 자연어를 보내면 **법제처 서버가 의미를 해석해 관련 조문을 랭킹해서** 돌려준다. 즉 **우리가 한국 법 전체를 따로 저장·색인할 필요가 없다.** `lib/law/`에 **법제처를 한 번 더 호출하는 함수만 추가**하면 된다. **DB 스키마 변경 없음.**

### 2-3. ✅ 실측 검증 완료 (확정 사실)

코드를 붙이기 전 **법제처 `target=aiSearch`를 실제 호출(OC=`kca-api`)** 해 가정을 확인했다. **성립한다.**

| 법령 이름 없는 질의 | `aiSearch` 실제 반환 | 같은 질의 `target=law` |
|---|---|---|
| "야근수당 안 주면" | 지방공무원 수당규정 제15·16조 등 | **0건** |
| "전세보증금 못 돌려받았어요" | **주택임대차보호법**(001248) 등 | **0건** |
| "직장 내 괴롭힘 신고" | **근로기준법** 제76조의3·제116조 | **0건** |

**검증으로 드러난 응답 스키마 (중요 — 기존 파서를 그대로 못 쓴다):**

| 구분 | `target=law` (현재 파서) | `target=aiSearch` (신규) |
|---|---|---|
| 래퍼 키 | `LawSearch` | **`aiSearch`** |
| 배열 키 | `law` | **`법령조문`** |
| 항목 단위 | 법령 | **조문** |
| 법령명 필드 | `법령명한글` | **`법령명`** |
| 법령ID | `법령ID` | `법령ID` (동일) |
| 조문 본문 | 없음(2차 호출 필요) | **`조문내용`·`조문제목`·`조문번호` 응답에 내장** |

→ **(1) 전용 파서 필요**(기존 `searchByName` 파서로는 빈 배열). **(2) 조문 본문이 검색 응답에 들어오므로 본문 회수용 2차 호출(`fetchArticles`) 없이 근거 조문 구성**(법령 전체 맥락이 필요할 때만 기존 `get_law_text` 사용). **(3) DB 변경 없음.**

> **품질 주의**: `aiSearch`는 의미 랭킹이라 곁가지 법령을 위로 낼 수 있다(예: "야근수당"→근로기준법보다 지방공무원 수당규정이 먼저). 그래서 1위만 쓰지 말고 **상위 2~3건을 근거 후보**로 두고, 답변의 조문 인용은 **`verifyCitations`로 교차검증**해 환각을 막는다(이미 흐름에 있음).

### 2-4. 내장 설계 (최소 변경)

| 단계 | 작업 | 위치 | DB |
|---|---|---|---|
| 1 | `DrfTarget`에 `"aiSearch"` 추가 | `lib/law/client.ts:23` | — |
| 2 | **전용 파서 `parseAiSearchList`** 신설 — `aiSearch.법령조문[]` → `LawRef`(법령ID로 중복 제거). 판례 파서(`decisions.ts:54`)의 방어적 패턴 차용 | `lib/law/search.ts` | — |
| 3 | `searchAiLaw(query)` 신설 — `target=aiSearch` 호출 → `LawLookup` 반환. **조문 본문은 응답의 `조문내용`에서 직접** 구성(2차 호출 없음) | `lib/law/search.ts` | — |
| 4 | `searchLaw`에서 **이름 검색 0건일 때 `searchAiLaw`로 폴백** | `search.ts:192` (`refs.length===0` 분기) | — |
| 5 | 캐싱·환각차단 **자동 재사용** — `cachedGetJson(tool_name="search_ai_law")`, `verifyCitations`는 답변 텍스트만 보므로 그대로 적용 | `cache.ts`·`verify.ts` | — |
| 6 | **route.ts 무변경** — `searchLaw`가 같은 `LawLookup`을 반환하면 분기·참조문서·검증 흐름 그대로 작동 | `route.ts:202` | — |

> **MVP = 폴백 내장형.** `searchLaw` 안에서 "이름 검색 실패 → 의미검색"으로 떨어지게 하면 `route.ts`는 한 글자도 안 바뀐다. 품질을 보고 나중에 "이름 검색과 의미검색을 항상 병행해 합치기"로 올릴 수 있다.

### 2-5. 손대는 파일 (백엔드)

```
lib/law/
├─ client.ts ··· [1] DrfTarget 에 "aiSearch" 추가
├─ search.ts ··· [2] parseAiSearchList 신설 · [3] searchAiLaw 신설 · [4] searchLaw 0건 폴백
├─ cache.ts ···· [참고] 그대로 재사용 (tool_name="search_ai_law")
└─ verify.ts ··· [참고] 인용 검증 — 그대로 적용
app/api/chat/route.ts ··· [참고] 무변경 (LawLookup 계약 유지)
supabase/migrations/ ····· 변경 없음 (의미검색은 법제처가 수행)
```

### ▶ 사용자 프롬프트 — 2장 (그대로 복사해 시키기)

> 지금 우리 챗봇은 사람들이 **"근로기준법"처럼 법 이름을 정확히 말해야만** 관련 법을 찾아 줘. 그런데 **"야근시키고 수당을 안 줘요", "전세금을 못 돌려받았어요"** 처럼 **법 이름 없이 상황만 말하면 관련 법을 못 찾고 그냥 넘어가.** 법제처에는 **법 이름이 아니라 조문 내용의 '의미'로 찾아 주는 검색(본문 의미검색, aiSearch)**이 따로 있어(우리가 직접 호출해서 잘 되는 걸 확인했어). 우리 코드에서 **법 이름으로 검색했는데 아무것도 안 나오면, 이 의미검색으로 한 번 더 찾아보도록** 이어 줘.
>
> 한국 법을 우리가 따로 **임베딩 색인으로 저장할 필요는 없고**(법제처가 의미 분석을 직접 해 줘), **법제처에 한 번 더 물어보기만** 하면 돼. 이 검색은 돌아오는 결과 모양이 기존과 좀 달라(법 한 건씩이 아니라 **조문 단위**로 오고, 조문 본문이 같이 와) — 그 모양에 맞춰 읽어 주고, **같이 온 조항 본문을 근거로** 써 줘. 결과가 여러 개면 가장 관련 있어 보이는 **위쪽 2~3개**를 근거 후보로 같이 보여 줘. 그리고 **기존에 법 이름으로 잘 찾던 질문은 지금처럼 그대로** 동작하게 두고, 새로 찾은 결과도 **인용이 진짜인지 확인하는 기존 검증**을 똑같이 거치게 해 줘.

> 붙이기 전 한 번 시켜 볼 것: "야근수당 안 주면" 같이 **법 이름이 없는 질문**을 넣어서, 근로기준법 같은 **맞는 법의 조항이 실제로 돌아오는지** 먼저 확인해 줘.

---

# 후반부 — 화면(UI) 재구성

> 백엔드(2장)가 끝나 **검색이 제대로 찾기 시작하면**, 이제 그 결과를 사용자가 잘 보고 신뢰하도록 화면을 다듬는다.

## 3. [화면] 서버가 보내는 신호를 화면에 드러내기 (+ 공용 타입)

서버(`route.ts`)는 답변과 함께 **두 신호**를 이미 보낸다.

- **`routing`(분기 결과)**: 이 답을 내부규정으로 했는지 / 법령으로 했는지 + 관련도 점수.
- **`citations`(인용 검증)**: 답변의 법조문 인용을 법제처 DB와 대조한 결과(✓ 실존 / ✗ 없음 / ⚠ 확인불가)와 환각 여부.

그런데 화면(`page.tsx:17-21`)은 `StreamEvent` 타입에 이 두 줄이 없어 **그냥 흘려버린다.** 이걸 받아서:

- 답변 위에 **분기 줄** — "📘 내부규정 근거" 또는 "⚖️ 법령 근거(관련도 N%)".
- 답변 아래에 **인용 검증 줄** — ✓/✗/⚠.
- 확인 안 된 인용이 하나라도 있으면(`hasHallucination`) **눈에 띄는 경고 배너**.
- (작은 백엔드 1개) **진행 상태(`stage`)** — 답 시작 전 "내부 규정 찾는 중… / 법령 확인 중…" 한 줄. 이것만 서버에 단계 신호를 새로 추가한다.

그리고 같은 데이터 모양(`StreamEvent`·`SourceChunk`)을 화면·서버가 **각자 따로** 적어 둬서 어긋난다(이번에 신호를 흘린 것도 이 때문). **`lib/types.ts` 한 곳에 모아** 양쪽이 함께 쓰게 정리한다.

> **이게 후반부 1순위인 이유**: `routing`·`citations`는 **이미 서버가 보낸다.** 화면이 받아 그리기만 하면 된다 — **새 백엔드 작업은 진행 상태(`stage`) 하나뿐.**

### ▶ 사용자 프롬프트 — 3장

> 우리 챗봇 서버는 답을 보낼 때 **이 답을 '내부 규정'으로 했는지 '법령'으로 했는지**, 그리고 **답에 나온 법 조항이 진짜 있는 조항인지 공식 원문과 대조한 결과(인용 검증)**를 같이 보내고 있어. 그런데 화면이 이걸 안 받고 버려서 사용자에게 안 보여. 화면이 이 두 가지를 받아서, **답 위에 '📘 내부 규정으로 답함' 또는 '⚖️ 법령으로 답함(관련도 N%)'** 을 한 줄로 보여 주고, **답 아래에 '✓ 확인된 인용 / ✗ 확인 안 된 인용 / ⚠ 확인 불가'** 를 보여 줘. **확인 안 된 인용이 하나라도 있으면(= AI가 지어낸 환각 징후)** 눈에 띄게 **"일부 인용을 확인할 수 없어요 — 원문을 확인하세요"** 라는 경고 띠를 띄워 줘.

> 그리고 답이 나오기 시작하기 전에 **지금 무엇을 하는 중인지** 한 줄로 보여 줘 — 규정을 찾는 중이면 "내부 규정 찾는 중…", 법을 확인하는 중이면 "법령 확인 중…"처럼. 이건 서버가 지금 어느 단계인지 같이 알려 줘야 하니 **그 신호도 같이** 만들어 줘.

> 마지막으로, 화면과 서버가 **똑같은 데이터 모양을 각자 따로** 적어 둬서 자꾸 어긋나는데(방금 그 신호를 화면이 흘린 것도 이 때문), 그 모양을 **한 파일에 모아** 양쪽이 같이 쓰게 정리해 줘. 화면 동작은 그대로 두고.

---

## 4. [화면] 근거 보여 주는 방식 재구성

새로 들어온 판례까지 합쳐, 근거를 **한 화면에서 일관되게** 본다.

- **출처 뱃지 통일**: 지금 참조 **목록**은 회색 글씨 뱃지("법제처 법령"/"판례"/"N% 관련도")인데, **패널과 같은 색 뱃지**로 통일한다 — 📘 파랑 내부규정 / ⚖️ 보라 법령·판례. (색 뱃지는 `source-panel.tsx`엔 이미 있으니 같은 기준을 목록에도 적용.)
- **본문 `[1][2]` → 패널 점프**: 답변 본문의 근거 번호를 누르면, 오른쪽(좁은 화면은 아래) 패널이 그 근거 원문으로 이동.
- **법령 카드에 인용 검증 배지**: 법령 근거 카드 안에 그 조문의 ✓ 확인됨 / ✗ 확인 안 됨 / ⚠ 확인 불가 표시(3장의 `citations`와 연결).
- **판례 카드 정합**: 0-2에서 새로 들어온 ⚖️ 판례 카드를 같은 패널·뱃지 체계에 맞춘다.

### ▶ 사용자 프롬프트 — 4장

> 참조 문서 **목록**에서 내부 규정·법령·판례를 **색이 있는 같은 모양 뱃지**로 통일해 줘 — **내부 규정은 파란 📘, 법령과 판례는 보라 ⚖️** (지금 목록은 회색 글씨라 구분이 약해). 그리고 답 본문에 있는 **근거 번호 [1] [2]를 누르면** 오른쪽(좁은 화면은 아래) 패널이 **그 근거 원문으로 이동**하게 해 줘. **법령 근거 카드 안에는** 그 조항이 진짜 있는지 확인한 표시(**✓ 확인됨 / ✗ 확인 안 됨 / ⚠ 확인 불가**)도 같이 보여 줘. 지금 잘 되는 패널 열고 닫기는 그대로 두고.

---

## 5. [화면] 쓰기 편하게 — 생성 제어·편의

비전문가(외부 기관 담당자)가 쓰는 화면이라, 작은 매끄러움이 신뢰로 이어진다.

| 기능 | 무엇을 | 왜 | 현재 |
|---|---|---|---|
| **답변 복사** | 답변마다 '복사' 버튼 | 실무에서 바로 붙여 사용 | 없음 |
| **예시 질문 칩** | 빈 화면에 "이런 걸 물어보세요" 버튼 | 처음 쓰는 사람이 막막하지 않음 | 빈 화면 문구만 있음 |
| **생성 중단** | 생성 중 '전송'이 '멈춤'으로 바뀌고 누르면 즉시 멈춤 | 길거나 엉뚱할 때 바로 끊고 재질문, 비용 절감 | 없음(중단 불가) |
| **맨 아래로** | 위로 올려 읽다 새 글이 이어지면 '↓ 최신으로' 버튼 | 긴 답변·법령 원문에서 길 잃지 않음 | 스크롤 추적만 있고 버튼 없음 |

> '생성 중단'은 지금 `send()`에 요청을 취소할 수단이 없어서 안 된다 — **`AbortController`**(진행 중인 요청을 도중에 끊는 표준 수단)를 붙여 '멈춤' 버튼이 그걸 호출하게 한다. 나머지(복사·예시칩·맨아래로)는 **화면만으로** 끝난다.

### ▶ 사용자 프롬프트 — 5장

> 챗봇 화면에 네 가지 편의를 더해 줘. ① **답마다 '복사' 버튼**으로 한 번에 복사, ② 대화가 비어 있을 때 **'이런 걸 물어보세요' 같은 예시 질문 버튼** 몇 개, ③ 답이 만들어지는 동안 **'전송' 버튼이 '멈춤'으로 바뀌고 누르면 바로 멈추기**(멈춘 뒤 곧장 다음 질문 가능), ④ 위로 올려 읽다가 새 답이 이어지면 **'↓ 최신으로' 버튼**이 떠서 한 번에 맨 아래로(맨 아래에 있을 땐 안 보이게). 지금 잘 되는 대화·스크롤·패널은 **그대로 두고** 이 네 가지만 더해 줘.

---

## 6. 구현계획 (단계별) + 파일 맵

작은 것·위험 낮은 것부터. **각 단계 끝에 `pnpm build` 확인.** 백엔드(검색)를 먼저, 화면을 뒤에.

| 단계 | 묶음 | 작업 | 손대는 파일 | 신규 백엔드 |
|---|---|---|---|---|
| **1** | [백엔드] 2장 | `DrfTarget`에 `aiSearch` + `parseAiSearchList` + `searchAiLaw` + `searchLaw` 0건 폴백 | `lib/law/client.ts`, `lib/law/search.ts` | 없음(법제처 호출만) |
| **2** | [화면] 3장 | `lib/types.ts` 공용 타입 + `routing`·`citations` 수신·표시 + 진행상태 | `lib/types.ts`(신규), `app/page.tsx`, `app/api/chat/route.ts` | **`stage` 이벤트** |
| **3** | [화면] 4장 | 목록 색뱃지 통일 · 본문 `[1][2]` 점프 · 법령 카드 검증 배지 · 판례 카드 정합 | `app/page.tsx`, `components/ui/source-panel.tsx` | 없음 |
| **4** | [화면] 5장 | 복사 · 예시칩 · 생성중단(AbortController) · 맨아래로 | `app/page.tsx`, `components/ui/response.tsx` | 없음 |

```
lib/
├─ law/client.ts ········ [1] target에 "aiSearch"
├─ law/search.ts ········ [1] parseAiSearchList·searchAiLaw·searchLaw 폴백
├─ law/cache.ts ········· [참고] 재사용(tool_name="search_ai_law")
├─ law/verify.ts ········ [참고] 인용 검증 그대로
└─ types.ts ············· [2] (신규) 공용 타입(Message·SourceChunk·StreamEvent: sources·routing·delta·citations·stage·done·error)
app/
├─ page.tsx ············· [2·3·4] 신호 수신·표시, 목록 뱃지·점프·검증배지, 복사·예시칩·중단·맨아래로
└─ api/chat/route.ts ···· [1 무변경 · 2 stage 이벤트] StreamEvent를 lib/types에서 import
components/ui/
├─ source-panel.tsx ····· [3] 법령 카드 검증 배지
└─ response.tsx ········· [4] 복사 버튼
supabase/migrations/ ···· 변경 없음(DB 스키마 변경 없음)
```

> **순서가 핵심**: 1단계(검색 강화)가 사용자 체감의 큰 구멍을 메우므로 먼저. 2단계에서 공용 타입을 세워 두면 3~4단계에서 화면·서버가 어긋날 일이 없다.

---

## 7. 핵심 용어 (한 줄 정의)

| 용어 | 한 줄 설명 |
|---|---|
| **본문 의미검색 / aiSearch** | 법령 **이름**이 아니라 **조문 내용**을 자연어로 찾는 법제처 검색. "야근수당 안 주면"으로 근로기준법을 찾아낸다. 의미검색은 법제처 서버가 수행한다. |
| **폴백 내장형** | 법령 **이름 검색이 0건일 때만** 의미검색으로 떨어지는 방식. 화면·서버 흐름을 안 바꾸는 최소 변경. |
| **`routing` / `citations`** | 분기 결과(내부규정/법령·관련도) / 인용 검증 결과(✓✗⚠·환각 여부) 스트림 이벤트. **서버는 보내는데 화면이 아직 안 받는** 두 신호. |
| **`stage`(진행 상태)** | 답 시작 전 "지금 무엇을 하는 중"인지 알리는 서버 신호. 이번에 새로 추가하는 유일한 백엔드 이벤트. |
| **인용 검증 / 환각** | AI가 댄 "○○법 제N조"를 공식 원문과 대조해 맞는 것만 남기는 것 / AI가 없는 내용을 사실처럼 지어내는 현상. |
| **판례 / 헌재결정** | 법령 분기 시 함께 조회하는 법원 판례(`prec`)·헌법재판소 결정(`detc`). 판시사항·판결요지를 근거로 보강. |
| **공용 타입(lib/types.ts)** | 화면·서버가 함께 쓰는 데이터 모양. 지금은 세 파일에 흩어져 중복 — 한 곳으로 모은다. |
| **`AbortController`** | 진행 중인 요청을 도중에 취소하는 표준 수단. '생성 중단'에 쓴다. |

---

## 8. 직접 만져보며 복습하기 (회차 후)

1. **법 이름을 댄 질문**("근로기준법 연장근로 한도")과 **법 이름을 안 댄 질문**("야근수당 떼였어요") 둘 다에서 **법령 근거가 붙는지** 확인한다(2장의 핵심).
2. 내부규정에 있는 질문 → 답변 위 **'📘 내부규정 근거'**, 없는 법령 질문 → **'⚖️ 법령 근거(관련도 N%)'** 로 바뀌는지 확인(분기 줄).
3. 일부러 **없는 법조문**으로 유도해, 답변 아래 **'✗ 확인 안 됨' + 경고 배너**가 뜨는지 확인(인용 검증).
4. 법령 분기 질문에서 **판례 카드(⚖️ 판례)** 가 근거에 함께 뜨고, 참조 **목록·패널 뱃지 색이 통일**됐는지 확인.
5. 답변 본문의 `[1][2]`를 눌러 **패널이 해당 근거로 이동**하고, **복사·예시칩·중단·↓최신으로**가 동작하는지 확인.

---

> 이 회차를 마치면 **(전반부) 법 이름을 몰라도 상황만으로 법령 근거를 찾고, (후반부) 백엔드가 보내던 신호가 화면에 드러나며 근거·편의가 정리된다.** 이로써 어드바이저 ①은 "내부규정 → 법령(이름·의미검색) → 판례 → 인용 검증"이 한 흐름으로 완성된다. 다음은 7월 — **Vercel 오픈 환경설정과 오픈 전 보안 하드닝**(로그인 게이트·레이트리밋·색인 API 차단·에러 일반화)이다.
